//! The `http_rest` dispatcher for `ScalarPathBoundService`, expanded out of the transport's macro
//! into a module of its own.

use crate::scalar_path_bound_service::GetDocumentError;

scalar_path_bound_service_http_rest_dispatcher!();
